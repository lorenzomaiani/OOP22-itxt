package org.example.controller.file;

/**
 * Explain different type of Open Operation.
 */
public enum OpenType {
    /**
     * Open a new file, so has to set info and load information.
     */
    FILE,
    /**
     * Acquire text to another file, no needed to set anything.
     */
    ACQUISITION;
}
